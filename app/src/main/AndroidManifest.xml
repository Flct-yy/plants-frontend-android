<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Plantsfrontendandroid">
        <activity
            android:name=".Ui.Activity.EditArticles.EditArticlesActivity"
            android:exported="false" />
        <activity
            android:name=".Ui.Activity.ArticleDetails.ArticleDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".Ui.Activity.Login.ForgotActivity"
            android:exported="false" />
        <activity
            android:name=".Ui.Activity.Register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".Ui.Activity.Login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".Ui.MainActivity"
            android:exported="true">
            <intent-filter>

                <!-- 标记为入口文件 -->
                <action android:name="android.intent.action.MAIN" />
                <!-- 显示在启动器中 -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>